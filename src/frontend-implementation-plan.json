{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Publish Draft Version 25 to Live (radicaleconomist101-h78.caffeine.xyz)",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Ensure Live publish workflow runs preflight then deploys the current Draft (Version 25) to the Live domain without wiping backend state, and that the UI environment indicator shows Live.",
      "acceptanceCriteria": [
        "The preflight script (frontend/scripts/deployment-preflight.sh) completes successfully (exit code 0).",
        "The Live publish script completes successfully and deploys the frontend canister to the IC network without reinstalling/wiping backend state.",
        "Visiting https://radicaleconomist101-h78.caffeine.xyz loads the latest build and the UI indicates the environment is Live."
      ],
      "file_operations": [
        {
          "path": "frontend/scripts/publish-live.sh",
          "operation": "modify",
          "description": "Harden the Live publish script so it explicitly deploys ONLY the frontend canister in a non-destructive mode (preserving backend state), and keep the target domain radicaleconomist101-h78.caffeine.xyz unchanged."
        },
        {
          "path": "frontend/scripts/deployment-preflight.sh",
          "operation": "modify",
          "description": "Ensure preflight output and checks clearly validate Live build configuration (including VITE_APP_ENV=Live) needed for the UI environment badge to show Live, without changing deployment behavior."
        },
        {
          "path": "frontend/docs/deployment-preflight.md",
          "operation": "modify",
          "description": "Update the preflight documentation to align with the current Live publish flow and explicitly call out validation that the app’s environment badge reads 'Live' after a production build."
        },
        {
          "path": "frontend/.env.production",
          "operation": "modify",
          "description": "Verify production environment configuration remains set to Live (VITE_APP_ENV=Live) so production builds render the Live environment indicator."
        }
      ]
    },
    {
      "id": "REQ-2",
      "summary": "Update Live publish documentation and script console output to reference Version 25 (not Version 22) while keeping the Live target domain unchanged.",
      "acceptanceCriteria": [
        "frontend/scripts/publish-live.sh console messages refer to Version 25 (not Version 22).",
        "frontend/docs/publish-live.md references Version 25 (not Version 22) and still targets radicaleconomist101-h78.caffeine.xyz.",
        "No deployment behavior changes are introduced beyond updating the displayed version text."
      ],
      "file_operations": [
        {
          "path": "frontend/scripts/publish-live.sh",
          "operation": "modify",
          "description": "Replace all user-facing console output and comments that say 'Version 22' with 'Version 25' and keep the target domain radicaleconomist101-h78.caffeine.xyz unchanged."
        },
        {
          "path": "frontend/docs/publish-live.md",
          "operation": "modify",
          "description": "Update the publish documentation to reference Draft Version 25 (not 22) while preserving the target domain radicaleconomist101-h78.caffeine.xyz; also fix/complete the document content if it is currently truncated so it renders correctly."
        }
      ]
    }
  ]
}