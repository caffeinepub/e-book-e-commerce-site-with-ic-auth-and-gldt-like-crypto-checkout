{
  "kind": "build_request",
  "title": "Revert homepage hero image to the previous asset",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update the homepage hero image to use the previous static asset (/assets/generated/phoenix-1988-image-3.dim_564x483.png) instead of the current one (/assets/generated/home-hero-replacement.dim_564x483.png). Keep the image dimensions at 564x483 and ensure the page renders without broken-image errors.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-3"
        ],
        "quotes": [
          "just replace the current one with the previous one since clearly the one i'm uploading is being censored"
        ]
      },
      "acceptanceCriteria": [
        "In frontend/src/pages/HomePage.tsx, the hero <img> src points to /assets/generated/phoenix-1988-image-3.dim_564x483.png.",
        "The homepage loads successfully and displays the hero image at 564x483 without a missing-asset/broken image icon.",
        "No other pages (checkout/KYC flow, book covers, admin pages) are changed as part of this update."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui (read-only).",
    "Do not edit any paths listed in frontend.immutablePaths (read-only).",
    "Keep all backend logic unchanged (no Motoko changes required)."
  ],
  "nonGoals": [
    "Diagnose or change any image upload/processing/censorship behavior for user-uploaded images beyond reverting the homepage hero asset reference.",
    "Any changes to the KYC flow or checkout behavior."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}