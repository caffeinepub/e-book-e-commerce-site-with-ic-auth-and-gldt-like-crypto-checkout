# Production environment configuration
# This file is used during production builds (vite build)
# Ensures the environment badge displays "Live" on production
VITE_APP_ENV=Live

# Version for Live builds (enforced by Live publish workflow)
VITE_APP_VERSION=29
